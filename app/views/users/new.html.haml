= form_for @user do |f|
  - if @user.errors.any?
    - @user.errors.full_messages.each do |msg|
      = msg

  = f.label :name
  = f.text_field :name

  = f.label :password
  = f.password_field :password

  = f.submit
