= form_for @entry do |f|
  - if @entry.errors.any?
    - @entry.errors.full_messages.each do |msg|
      = msg

  = f.label :title
  = f.text_field :title

  = f.label :body
  = f.text_area :body

  = f.submit
