.change_entry
  = form_for @entry do |f|
    - if @entry.errors.any?
      - @entry.errors.full_messages.each do |msg|
        = msg

    %p
      = f.text_field :title

    %p
      = f.text_area :body

    %p
      = f.submit
